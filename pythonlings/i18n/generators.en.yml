en:
  generators1: |
    Replace return [] with a loop using yield:
        for i in range(n, 0, -1): yield i
    Or the one-liner: yield from range(n, 0, -1)
  generators2: |
    Replace pass with: yield from it
    That's the entire fix — one line inside the for loop.
  generators3: |
    In __next__:
        if self.a > self.limit: raise StopIteration
        value = self.a
        self.a, self.b = self.b, self.a + self.b
        return value
  generators4: |
    Replace return with a while loop:
        n = start
        while True: yield n; n += 1
    Or: yield from itertools.count(start)
