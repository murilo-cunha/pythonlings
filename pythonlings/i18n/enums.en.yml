en:
  enums1: |
    Inside Direction(Enum):
        NORTH = 1
        SOUTH = 2
        EAST = 3
        WEST = 4
  enums2: |
    Inside Priority(IntEnum):
        LOW = auto()
        MEDIUM = auto()
        HIGH = auto()
        CRITICAL = auto()
  enums3: |
    to_hex: return f"#{r:02x}{g:02x}{b:02x}"
    from_hex is already implemented correctly — just fix to_hex.
