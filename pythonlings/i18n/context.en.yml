en:
  context_managers1: |
    with open(tmp_path, "w") as f: f.write("Hello, World!")
    with open(tmp_path) as f: content = f.read()
  context_managers2: |
    In __exit__: self.elapsed = time.monotonic() - self.start
    The _time import at class level is already there — use time.monotonic() directly.
  context_managers3: |
    After the yield line, add: items.sort()
    Code after yield in a @contextmanager runs on __exit__ (after the with block).
  context_managers4: |
    Replace result2 = 1 / 0 with:
        with suppress(ZeroDivisionError):
            result2 = 1 / 0
    The suppress context manager silently ignores the ZeroDivisionError.
